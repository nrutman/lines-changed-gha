name: 'Lines Changed Summary'
description: 'Comment on PRs with a lines changed summary, excluding specified file patterns'
author: 'Your Name'
branding:
  icon: 'bar-chart-2'
  color: 'blue'

inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
    default: ${{ github.token }}
  exclude-patterns:
    description: 'Comma-separated list of file patterns to exclude (e.g., "**/generated/**,**/*.generated.ts")'
    required: false
    default: ''
  comment-header:
    description: 'Header text for the comment'
    required: false
    default: '## Lines Changed Summary'

outputs:
  added-lines:
    description: 'Number of lines added (excluding filtered files)'
  removed-lines:
    description: 'Number of lines removed (excluding filtered files)'
  total-files:
    description: 'Total number of files changed'
  excluded-files:
    description: 'Number of files excluded by patterns'

runs:
  using: 'node20'
  main: 'dist/index.js'
